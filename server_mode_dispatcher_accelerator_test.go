//go:build accelerator

package main

// The Accelerator binary deliberately contains only its six policy methods,
// so compatibility-route status assertions are not meaningful in this build.
const ordinaryDispatcherBuild = false
